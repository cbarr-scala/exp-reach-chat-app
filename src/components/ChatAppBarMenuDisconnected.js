import React from 'react';
import FlatButton from 'material-ui/FlatButton';

const ChatAppBarMenuDisconnected = props =>
  (
    <FlatButton {...props} label="Connect!" />
  );

export default ChatAppBarMenuDisconnected;
